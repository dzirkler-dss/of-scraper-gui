name: Build OF-Scraper GUI image

on:
  push:
    branches: [main]
    paths:
      - 'docker/**'
      - 'OF-Scraper-3.14.7/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: dzirkler-dss/of-scraper-gui

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=3.14.7,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Stage patch scripts into version dirs (upstream Dockerfile expects them there)
        run: |
          for v in 3.12.9 3.14.3 3.14.5 3.14.7; do
            cp "patch_ofscraper_${v}_gui.py" "OF-Scraper-${v}/"
          done

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          build-args: |
            GUI_PATCH_VERSION=3.14.7
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
